const data = {
  N2: [.2, .2, .2, .2, .2],
  P7: [.2, .2, .2, .2, .1, .1],
}

$(() => {
  // Step 1:

  $('button').click(e => {
    const code = $(e.target).data('code')
    // Step 2:

    // Step 3:

    // Step 4:
  })
  $('button:first-child').click()

})

// vi:et
